var mongoose = require("mongoose");

//Reference the Schema cnstructor. 
//This is fairly similar to sequeize.
var Schema = mongoose.Schema; 

//Export the Article module when finished
module.exports = "Article";